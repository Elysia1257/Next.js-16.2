[pytest]
markers =
    vidu_real: Real Vidu API integration test (requires VIDU_API_KEY env var, excluded from CI by default)

    vidu_e2e: Full Vidu production chain verification (creates real Vidu tasks, long-running)

